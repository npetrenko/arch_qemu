# these constants are not really customizable, because some of them are hardcoded into .sh scripts

# quemu image path on the host OS
IMAGE_PATH = "/tmp/arch.raw"

# where to mount image during bootstrap phase
ROOT_FS_PATH = "/mnt"

# device which will be the mounted image
LOOP_DEVICE_PATH = "/dev/loop3342"
